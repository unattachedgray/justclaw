# /dev-ship — Phases 5-7: Test, commit, and reflect

Run Phases 5 (TEST), 6 (SHIP), and 7 (REFLECT) from `skills/dev/SKILL.md`.

Target: $ARGUMENTS (commit message hint, or empty to auto-generate)

For when you've already built and reviewed the code and want to finalize: run tests, commit, log learnings.
