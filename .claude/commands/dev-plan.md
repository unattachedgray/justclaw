# /dev-plan — Phase 2 only: Design the solution

Run ONLY Phase 2 (PLAN) from `skills/dev/SKILL.md`.

Target: $ARGUMENTS

Assumes you've already done the Think phase (or have sufficient context). Output the file change list, test strategy, and risk assessment. Stop before Build.
