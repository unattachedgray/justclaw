# /dev — Structured development lifecycle

Read and follow the instructions in `skills/dev/SKILL.md`.

Mode and target: $ARGUMENTS

**Modes**: `new`, `fix`, `refactor`, `debug`. If no mode given, infer from context or ask.

**Examples**:
- `/dev new add webhook support for task completion events`
- `/dev fix session IDs not persisting after bot restart`
- `/dev refactor src/discord/bot.ts — extract progress rendering`
- `/dev debug heartbeat checks timing out intermittently`
