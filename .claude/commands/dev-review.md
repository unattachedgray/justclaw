# /dev-review — Phase 4 only: Self-review recent changes

Run ONLY Phase 4 (REVIEW) from `skills/dev/SKILL.md`.

Target: $ARGUMENTS (file paths, or empty to review all uncommitted changes)

Review the current diff (`git diff` or `git diff --cached`) using the Code Reviewer checklist. Report findings as Must Fix / Should Fix / Nits. Fix Must Fix issues automatically.
