# /eval — Run skill evaluations against test cases

Read and follow the instructions in `skills/eval/SKILL.md`.

Evaluate: $ARGUMENTS

If no arguments provided, discover and run all evals found in `.evals/`.
